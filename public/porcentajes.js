function responderPregunta1(){
    estadoBoton = 0;
    //Para sumar al porcentaje de candidatura
    porcentajeCandidatura += Math.floor(Math.random() * (20 - 18 + 1) ) + 18;
    btn1.remove();
    btn2.remove();
    video.play();
    video.showControls();
    panelPreguntas.hide();
  }
  
  function responderPregunta2(){
    estadoBoton = 0;
    //Para sumar al porcentaje de candidatura
    porcentajeCandidatura += Math.floor(Math.random() * (18 - 16 + 1) ) + 16;
    btn1.remove();
    btn2.remove();
    video.play();
    video.showControls();
    panelPreguntas.hide();
  }
  
  function responderPregunta3(){
    estadoBoton = 0;
    //Para sumar al porcentaje de candidatura
    porcentajeCandidatura += Math.floor(Math.random() * (16 - 15 + 1) ) + 15;
    btn1.remove();
    btn2.remove();
    video.play();
    video.showControls();
    panelPreguntas.hide();
  }
  
  function responderPregunta4(){
    estadoBoton = 0;
    //Para sumar al porcentaje de candidatura
    porcentajeCandidatura += Math.floor(Math.random() * (15 - 14 + 1) ) + 14;
    btn1.remove();
    btn2.remove();
    video.play();
    video.showControls();
    panelPreguntas.hide();
  }
  